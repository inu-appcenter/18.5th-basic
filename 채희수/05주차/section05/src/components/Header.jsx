// Head 컴포넌트 (자식 컴포넌트)
const Header = () => {
  return (
    <header>
      <h1>header</h1>
    </header>
  );
};

// Head컴포넌트를 내보내주기 위함
// ES 모듈 시스템에 의해서 header.jsx파일에서 header라는 함수 컴포넌트가 기본 값으로 내보내짐
export default Header;
